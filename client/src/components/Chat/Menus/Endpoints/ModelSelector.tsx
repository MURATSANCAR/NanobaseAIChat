import type { ModelSelectorProps } from '~/common';

/** Model selection is hidden; NanobaseAI is the fixed default model. */
export default function ModelSelector(_props: ModelSelectorProps) {
  return null;
}
